services:
  app:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    restart: unless-stopped
    init: true
    ports:
      - "80:9090"
    networks:
      - app_network
    env_file: ../.env
networks:
  app_network:
    driver: bridge
